import { ReactionPickerDirective } from './reaction-picker.directive';

describe('ReactionPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactionPickerDirective();
    expect(directive).toBeTruthy();
  });
});
